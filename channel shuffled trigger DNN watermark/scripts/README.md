Utility scripts for reproducibility.

- `search_lora.py`: run the genetic search used to select dataset-specific LoRA ranks
- `smoke_test.py`: fast end-to-end sanity check for train, evaluate, and NAS
