This directory contains:

- architecture code used by the paper
- `checkpoints/` for generated reference and LoRA weights

The repository tracks only the code and the placeholder `checkpoints/.gitkeep`.
Generated `.pt`, `.json`, and `.csv` artifacts are ignored by `.gitignore`.
